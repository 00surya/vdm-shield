# Set these in Heroku Settings > Config Vars. Never commit real credentials.
# DATABASE_URL is supplied by an attached PostgreSQL database.
VDM_CLOUD_SECRET=
VDM_PUBLIC_URL=https://YOUR-ACTUAL-HEROKU-HOSTNAME
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
WEB_CONCURRENCY=2
